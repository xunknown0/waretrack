<style>
/* Full-page container */
.error-page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  background: #f0f4f8;
  text-align: center;
  font-family: 'Poppins', sans-serif;
  padding: 40px 20px;
  color: #334155;
}

/* 404 circle style */
.error-page .error-circle {
  width: 180px;
  height: 180px;
  border-radius: 50%;
  background: #2563eb;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 5rem;
  font-weight: 900;
  color: #fff;
  margin-bottom: 25px;
  animation: bounce 2s infinite;
}

/* Subtitle */
.error-page h3 {
  font-size: 2rem;
  margin-bottom: 15px;
  color: #1e293b;
}

/* Paragraph */
.error-page p {
  font-size: 1.1rem;
  color: #64748b;
  margin-bottom: 30px;
}

/* Button with animated arrow */
.error-page .btn-primary {
  position: relative;
  padding: 12px 32px 12px 28px;
  font-size: 1rem;
  font-weight: 600;
  border-radius: 50px;
  border: none;
  background: linear-gradient(135deg, #2563eb, #3b82f6);
  color: #fff;
  box-shadow: 0 6px 15px rgba(37, 99, 235, 0.4);
  overflow: hidden;
  transition: all 0.3s ease;
}

/* Arrow span */
.error-page .btn-primary .arrow {
  display: inline-block;
  margin-left: 10px;
  transition: transform 0.3s ease;
}

/* Hover effect */
.error-page .btn-primary:hover {
  background: linear-gradient(135deg, #1e40af, #2563eb);
  transform: translateY(-4px) scale(1.05);
  box-shadow: 0 8px 20px rgba(37, 99, 235, 0.5);
}

.error-page .btn-primary:hover .arrow {
  transform: translateX(5px);
}

.error-page .btn-primary:active {
  transform: translateY(0) scale(0.98);
  box-shadow: 0 4px 10px rgba(37, 99, 235, 0.3);
}

/* Bounce animation */
@keyframes bounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-20px); }
}

/* Responsive adjustments */
@media (max-width: 768px) {
  .error-page .error-circle {
    width: 120px;
    height: 120px;
    font-size: 3.5rem;
  }
  .error-page h3 {
    font-size: 1.5rem;
  }
  .error-page p {
    font-size: 0.95rem;
  }
}
</style>

<div class="error-page">
  <div class="error-circle">404</div>
  <h3>Oops! Page Not Found</h3>
  <p>The page you are looking for might have been removed or never existed.</p>
  <a href="/login" class="btn btn-primary">
    Go Back to Login <span class="arrow">→</span>
  </a>
</div>
